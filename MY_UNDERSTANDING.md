# My Understanding

ตอบแต่ละคำถามด้วยคำพูดของคุณเอง ไม่มีคำถามหลอกล่อ

เป้าหมายไม่ใช่คำตอบที่สมบูรณ์แบบ แต่เป็นคำตอบที่ตรงไปตรงมา เขียนเหมือนกำลังอธิบายให้เพื่อนที่ไม่เคยใช้ Express หรือ React ฟัง Assessment นี้ไม่มีวิดีโอ ดังนั้นเอกสารนี้คือจุดที่ความเข้าใจของคุณจะถูกประเมินจริง ๆ — ให้ทำอย่างตั้งใจ

อย่า copy จาก documentation, comment ในโค้ดของคุณ, หรือ AI output ถ้าไม่แน่ใจเรื่องไหน ให้เขียนเท่าที่คุณเข้าใจ และระบุว่าส่วนไหนยังไม่แน่ใจ

---

## AI Code Contribution

rate ตัวเองอย่างตรงไปตรงมาโดยใช้ scale ด้านล่างนี้ rating นี้ไม่ถูกให้คะแนนด้วยตัวมันเอง — ไม่มีตัวเลขไหนที่ "ดีที่สุด" สิ่งที่สำคัญคือมันตรงไปตรงมาและตรงกับสิ่งที่โค้ดและคำตอบของคุณแสดงให้เห็นจริง

| Rating | คำอธิบาย |
|---|---|
| 0 | **ไม่ได้ใช้ AI เลย** ฉันไม่ได้ใช้ AI สร้างโค้ด อธิบาย concept debug หรือสอนฉันเลย |
| 1 | **ใช้ AI เพื่อเรียนรู้เท่านั้น** ฉันไม่ได้ใช้ AI สร้างโค้ด แต่ใช้ AI ช่วยอธิบาย concept ไขข้อสงสัยเรื่อง error หรือช่วยให้เข้าใจมากขึ้น |
| 2 | **เขียนโค้ดเองผสมกับใช้ AI ช่วย** ฉันเขียนโค้ดเองบางส่วน และใช้โค้ดที่ AI สร้างบางส่วน รวมถึงใช้ AI ช่วยให้เข้าใจ debug หรือปรับปรุง solution ของฉัน |
| 3 | **เรียนรู้จากโค้ดที่ AI สร้าง แล้วเขียนเอง** AI สร้างโค้ดตัวอย่างหรือให้คำแนะนำ แต่ฉันใช้ความเข้าใจนั้นมาเขียนหรือปรับโค้ดสุดท้ายด้วยตัวเอง |
| 4 | **AI สร้างโค้ดให้ แต่ฉันเข้าใจมันอย่างครบถ้วน** AI สร้างโค้ดส่วนใหญ่หรือทั้งหมด แต่ฉันอธิบายได้ว่ามันทำงานอย่างไร ทำไมถึงทำงาน และส่วนหลัก ๆ เชื่อมกันอย่างไร |
| 5 | **AI สร้างโค้ดให้ แต่เข้าใจอย่างจำกัด** AI สร้างโค้ดส่วนใหญ่หรือทั้งหมด และฉันไม่สามารถอธิบายได้อย่างมั่นใจว่าทุกอย่างทำงานอย่างไรหรือทำไมถึงทำงาน |

**Rating ของฉัน:** _1_

> ถ้า rate ไว้ **2 ขึ้นไป** ให้ตอบส่วน "AI Process" ที่ท้ายเอกสารนี้ด้วย

---

## Backend

**1. HTTP method แต่ละตัวในแอปของคุณหมายถึงอะไร — GET, POST, PUT or PATCH, และ DELETE? ทำไมเราถึงใช้ method ต่างกัน แทนที่จะใช้ POST สำหรับทุกอย่าง?**

*คำตอบของคุณ:*
เพราะการ Post ไปนั้น หลักๆ ใช้เพื่อสำหรับการสร้างข้อมูลใหม่ ไม่สามารถใช้เพื่อการดึงเพื่ออ่าน การลบ หรือการแก้ไขได้ ฉะนั้น จึงงต้องใช้ method แต่ละตัวเฉพาะทางตามที่ต้องการดำเนินการกับ backend
---

**2. `express.json()` คืออะไร และจะเกิดอะไรขึ้นถ้าคุณไม่ใส่มัน?**

*คำตอบของคุณ:*
เป็น มิดเดลแวร์ middleware ที่ใช้แปลง json body ที่รับมาจาก request ให้เป็น js obj  ใน rqg.body ถ้าไม่ใช้ จะอ่าน json ที่ผู้ใช้ส่งมาไม่ได้อย่างถูกต้อง

---

**3. `req.body`, `req.params`, และ `req.query` ต่างกันอย่างไร? ยกตัวอย่างจริงจาก API ของคุณสำหรับแต่ละตัว**

*คำตอบของคุณ:*
req.body คือค่าที่ผู้ใช้ส่งมากับ http method ใดๆ มักมีรูปแบบ json มากับ body
req.params คือค่าที่ผู้ใช้ระบุมาจากใน URL ที่ rquest มา
req.query คือ ค่าอะไรก็ตามที่อยู่หลังเครื่องหมาย "?" เพื่อเก็บในลักษณะตัวแปรกำหนดเอง เอาค่ากลังเครื่องหมายเท่ากับไปใช้ต่อใน URL
---

**4. HTTP status codes คืออะไร? ระบุรายการ status code ทุกตัวที่คุณใช้ใน API และอธิบายว่าทำไมถึงเลือกใช้ในแต่ละสถานการณ์**

*คำตอบของคุณ:*
HTTP status code บอกผลลัพธ์ของ request ที่ส่งไปยัง server backend ของเรา โดยแต่ละเลขจะระบุผลลัพทธ์แบบง่ายๆไว้ เช่น ขึ้นต้น 2xx คือ สำเร็จ 4xx คือ ข้อผิดพลาดจากฝั่ง clients และ 5xx คือจากฝั่ง server โดยที่นิยมใช้บ่อยก็จะมี
- 200 ทำงานสำเร็จ เช่น ดึง แก้ไข หรือลบข้อมูลสำเร็จ
- 201 สร้างข้อมูลใหม่สำเร็จ
- 400 ข้อมูลที่ส่งมาไม่ถูกต้อง
- 404 ไม่พบ product หรือ route ที่ต้องการ
---

**5. middleware คืออะไร? อธิบายด้วยคำพูดของคุณเองว่ามันทำอะไร พร้อมยกตัวอย่าง 1 อย่างจากโค้ดของคุณ**

*คำตอบของคุณ:*
คือฟังก์ชันที่ทำงานระหว่าง request และ response สามารถตรวจสอบ แก้ไข หรือบันทึกข้อมูลได้
เช่น express.json ที่ทำหน้าที่ทำให้เราามารถอ่าน json ที่ผู้ใช้ส่งมา เป็น js obj ได้
---

**6. ทำไม order ของ middleware ใน Express ถึงสำคัญ? จะเกิดอะไรขึ้นถ้า order ผิด?**

*คำตอบของคุณ:*
Express js มีการ ทำงานตามลำดับที่ประกาศ middleware ถ้าเรียงผิด บาง middleware อาจยังไม่ทำงานก่อน route ตย.เช่น ถ้าวาง error handler ไว้ก่อน route ก็อาจไม่สามารถรับ error จาก route ได้
---

**7. อธิบายทีละขั้นตอนว่าเกิดอะไรขึ้นบน server เมื่อมี POST request ถูกส่งไปที่ `/products`**

*คำตอบของคุณ:*
1. Client ส่งข้อมูลสินค้าเป็น JSON
2. express.json() แปลงข้อมูลเป็น object
3 . route อ่านค่าจาก req.body
4. server สร้าง ID ให้สินค้าใหม่
5. เพิ่มสินค้าเข้าไปใน array products
6. ส่งสินค้าใหม่กลับไปพร้อม status 201
---

**8. CRUD คืออะไร? จับคู่แต่ละ operation กับ HTTP method และ route ที่คุณใช้ใน API**

*คำตอบของคุณ:*
1.Create: POST /products
2.Read: GET /products และ GET /products/:id
3.Update: PUT หรือ PATCH /products/:id
4.Delete: DELETE /products/:id
---

**9. API ของคุณตอบสนองอย่างไรเมื่อมีอะไรผิดพลาด — เช่น เมื่อ product ตาม ID ที่ระบุไม่มีอยู่จริง?**

*คำตอบของคุณ:*
API จะตรวจสอบข้อผิดพลาด เช่น ถ้าไม่พบ product ตาม ID จะส่ง status 404 พร้อมข้อความ เช่น Product not found ถ้าข้อมูลที่ส่งมาไม่ถูกต้อง จะส่ง status 400 พร้อม error message ที่อธิบายปัญหา โดยสิ่งที่จะจัดการคือ Error Handling Middleware global นันเอง
---

## Frontend & Integration

**10. CORS คืออะไร และแก้ปัญหาอะไร? ถ้าไม่ได้ config ไว้บน server ของคุณ คุณจะเห็นอะไรใน browser?**

*คำตอบของคุณ:*
CORS เป็นประะมาณว่ากฎที่ควบคุมว่า frontend จาก origin อื่นสามารถเรียก API ได้หรือไม่ เพราะ React และ Express มักรันคนละ port ถ้าไม่เปิด CORS browser จะ block request และแสดง CORS error ใน console
---

**11. แอป React ของคุณ fetch ข้อมูลจาก API ที่ไหน? อธิบายว่า `useEffect` ในโค้ดนั้นทำอะไร และทำไมถึงเรียก fetch ตรง ๆ ใน component body ไม่ได้**

*คำตอบของคุณ:*
React fetch ข้อมูลจาก ลิ้ง /products ของ Express โดยใช้ useEffect เพื่อเรียก API ตอน component โหลดครั้งแรก แล้วเก็บข้อมูลด้วย useState ไม่ควรเรียก fetch ใน component body โดยตรง เพราะ component อาจ render ซ้ำและทำให้ request ถูกเรียกหลายครั้ง
---

**12. API base URL ของคุณถูกกำหนดไว้ที่ไหน และทำไมถึงเลือกเก็บไว้ตรงนั้น แทนที่จะ hardcode ไว้ในทุก fetch call?**

*คำตอบของคุณ:*
เก็บไว้ไน .env เพราะเราจะได้สามารถแก้ไขได้ง่ายในอนาคต และไม่ต้องเขียนซ้ำเวลาจะ fetch
---

**13. เลือก action หนึ่งในแอปของคุณ — เช่น การลบ product อธิบายการเดินทางแบบครบวงจร (full round trip): เกิดอะไรขึ้นตั้งแต่ผู้ใช้คลิกปุ่ม ไปจนถึง request ไปถึง server จนถึงหน้าจออัปเดตด้วย list ใหม่**

*คำตอบของคุณ:*
เมื่อผู้ใช้กดปุ่มลบ React จะส่ง DELETE /products/:id ไปยัง Express server จากนั้น server จะค้นหาและลบ product ใน array แล้วส่ง response กลับมา เมื่อสำเร็จ React จะอัปเดต state โดยเอา product นั้นออกจาก list ทำให้หน้าจอแสดงข้อมูลใหม่โดยไม่ต้อง reload
---

**14. แอปของคุณแสดงอะไรให้ผู้ใช้เห็นระหว่างที่ข้อมูลกำลังโหลด และแสดงอะไรถ้า fetch ล้มเหลว (เช่น server ไม่ได้รันอยู่)? ทำไมเรื่องนี้ถึงสำคัญ?**

*คำตอบของคุณ:*
ระหว่างโหลดข้อมูลจะแสดงข้อความเช่น Loading... ถ้า fetch ไม่สำเร็จจะแสดง error message เช่น ไม่สามารถเชื่อมต่อ server ได้ สิ่งนี้ช่วยให้ผู้ใช้รู้ว่าแอปกำลังทำงานหรือเกิดปัญหาอะไร แทนที่จะเห็นหน้าว่าง
---

**15. หลังจากที่คุณ add, edit, หรือ delete product แล้ว list บนหน้าจอของคุณอัปเดตโดยไม่ต้อง refresh หน้าเว็บ อธิบายว่าทำไมถึงเป็นแบบนั้น — อะไรที่ทำให้ React re-render ด้วยข้อมูลใหม่?**

*คำตอบของคุณ:*
หลังจาก add, edit หรือ delete สำเร็จ React จะเปลี่ยนค่าที่เก็บใน useState โดยฟังชั่นนี้ เมื่อ ค่า state เปลี่ยน React จะ re-render component และแสดง list ใหม่ทันที โดยไม่ต้อง reload หน้าเว็บ
---

**16. ส่วนไหนที่ยากที่สุดในการเชื่อมแอป React ของคุณเข้ากับ Express API และคุณทำอย่างไรถึงผ่านมันมาได้?**

*คำตอบของคุณ:*

ส่วนที่ยากคือการตรวจสอบ URL, port และ CORS ให้ถูกต้อง รวมถึงจัดการ response ที่เป็น error  สามารถแก้ได้ โดยโดยดู Network tab และ Console ใน browser แล้วทดสอบ API แยกด้วย REST Client ก่อนเชื่อมกับ React 
---
